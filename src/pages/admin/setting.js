import '../../auth.js'; // 공통 헤더 및 인증 리디렉션 로직

// 관리자 페이지는 auth.js에서 1차적으로 권한을 확인하므로,
// 이 파일은 페이지별 추가 스크립트가 필요할 경우를 위해 존재합니다.
document.addEventListener('DOMContentLoaded', () => {
    // console.log('Admin settings page loaded.');
});